/*
This code defines an object which holds the project's global constants.
*/

// The object in question.
const CONSTANTS = {
    expectedPathToRunningLocallyMarker: "./RUNNING_LOCALLY_MARKER.txt",
    pathToLocalDB: "./databases/local.db"
};

// Exports.
module.exports = CONSTANTS;
